version: '3.8'

services:
  api:
    build: .
    container_name: devsecops-pipeline
    ports:
      - "8000:8000"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
